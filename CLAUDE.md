# 个人网站升级

## 边界
- 静态 GitHub Pages 网站，目标兼顾业务合作、职业背书和内容品牌。
- 只做本地开发；修改 CI/CD、公开部署、git push、删除文件前须单独获得用户批准。
- 不虚构经历、客户、项目结果、数字或文章。现有站点内容亦为待用户核实的陈述，不新增背书。
- 保留现有 assets 构建产物作迁移参考，不直接修改压缩文件。

## 目录约定
- index.html：首页静态入口。
- src/data/：个人资料、公开链接和案例等内容数据，英文 kebab-case 文件名。
- src/styles/：可维护的 CSS；src/scripts/：渐进增强交互。
- scripts/：构建与验证工具；public/：原样输出的站点资源。
- `.github/workflows/`：GitHub Pages 构建与部署工作流；改动须单独获得用户批准。
- dist/：生成的构建产物，不手工修改、不提交。
- docs/：内容缺口和维护说明；临时检查文件不放源码目录。
- 新增目录先在本文件说明用途；不自动清理原始文件或用户素材。

## 实现约定
- Vite + 静态 HTML/CSS/JavaScript，不引入客户端框架或后端。
- 静态正文须在无 JavaScript 时可读。存储和剪贴板失败须有降级。
- 所有本地链接适配 GitHub Pages /mywebsite/ 子路径。
- 保留浅色/深色主题、键盘焦点、skip link 和 reduced-motion 支持。
- 未提供文章、履历 PDF 和案例证据时，不创建假内容或失效下载入口。

## 验证
- npm run build：生产构建。
- npm run check：静态内容、引用资源和本地链接检查。
- npm run preview -- --host 127.0.0.1：本地预览。
- UI 检查覆盖 320、375、768、1440px；同时检查无 JS、明暗主题和键盘导航。
- 未执行的检查须在交付说明中明确，不把静态检查称作浏览器验证。
